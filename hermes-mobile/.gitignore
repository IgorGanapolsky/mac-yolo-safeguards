node_modules/
.expo/
dist/
coverage/
*.log
.DS_Store
.env
.env.local
firebase-dist/
credentials.json
ios-credentials/

# Generated native projects (EAS prebuild) — root only; keep fastlane/metadata/android|ios tracked
/android/
/ios/
docs/proofs

# Phone install coordination lock (runtime, per-machine)
.install-phone-release.lock
.install-phone-release.lock.meta
.install-phone-release.lockdir/
.install-phone-release.last
.reassure/

# Higgsfield / ImageMagick scratch (canonical outputs live under fastlane/metadata)
docs/store-assets/higgsfield/_*.js
docs/store-assets/higgsfield/_*.png

# Human-presence device lease (transient, self-expiring) — never commit
.phone-human-hold
