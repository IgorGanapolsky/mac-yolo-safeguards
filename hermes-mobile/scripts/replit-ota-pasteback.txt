Production OTA for Hermes Mobile already lives on GitHub repo IgorGanapolsky/mac-yolo-safeguards (monorepo, app at hermes-mobile/, NOT standalone). Pick Option 3 — user wires GitHub + EXPO_TOKEN themselves — because that repo already has EXPO_TOKEN set and mobile-ota.yml on PR #73 (commit e27d28db) publishing to EAS channel production on push to main. EAS project ID 4ed13e30-9b97-4ddd-8a12-59106cae90d6, runtimeVersion policy appVersion. Replit is dev/preview only: merge fixes to mac-yolo-safeguards main for production OTA; do not duplicate production channel from Replit unless intentional. If Replit pushes its own separate GitHub monorepo with app at artifacts/hermes-mobile, use Option 1 and copy mobile-ota-replit-monorepo.yml.example from mac-yolo-safeguards (.github/workflows/) with paths artifacts/hermes-mobile/**, working-directory artifacts/hermes-mobile, and prefer preview channel — not production. Standalone hermes-mobile at repo root (Option 2) is not our layout; see hermes-mobile/docs/OTA_STANDALONE_REPO.md if needed.
