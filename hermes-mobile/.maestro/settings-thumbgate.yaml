# Settings — scroll through ThumbGate / Leash region to save button
appId: com.iganapolsky.hermesmobile
---
- launchApp
- runFlow: e2e-bootstrap.yaml
- tapOn: "Settings"
- extendedWaitUntil:
    visible:
      id: "SETTINGS"
    timeout: 30000
- scrollUntilVisible:
    element:
      id: "leash-smoke-test"
    direction: DOWN
    timeout: 90000
- assertVisible:
    id: "leash-smoke-test"
- scrollUntilVisible:
    element:
      id: "save-settings-button"
    direction: DOWN
    timeout: 60000
- assertVisible:
    id: "save-settings-button"
