#!/usr/bin/env bash
# vellum-yolo — CLI wrapper for Vellum Hybrid AI Assistant Engine
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec node "${REPO_ROOT}/tools/vellum-hybrid-engine.js" "$@"
