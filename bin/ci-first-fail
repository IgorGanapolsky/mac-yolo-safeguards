#!/usr/bin/env node
'use strict';

const { main } = require('../tools/ci-first-fail');

try {
  process.exitCode = main();
} catch (error) {
  process.stderr.write(`${error.message}\n`);
  process.exitCode = 1;
}
