#!/usr/bin/env bash
# bin/structural-refactor — SWE-Bench ProMax Structural Refactoring CLI
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec node "$SCRIPT_DIR/tools/structural-refactoring-engine.js" "$@"
