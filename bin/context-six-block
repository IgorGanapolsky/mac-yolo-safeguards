#!/usr/bin/env node
'use strict';

const { main } = require('../tools/context-six-block');

try {
  process.exitCode = main(process.argv.slice(2));
} catch (error) {
  process.stderr.write(`${error.message}\n`);
  process.exitCode = 1;
}
