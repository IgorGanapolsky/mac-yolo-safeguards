#!/usr/bin/env bash
# qoder-harness — CLI wrapper for Qoder-Style Autonomous Agent Harness & Convergence Engine
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec node "${REPO_ROOT}/tools/qoder-agent-harness.js" "$@"
