#!/usr/bin/env bash
# quality-gate — CLI wrapper for LLM Quality Engineering & Anti-Slop Evaluator
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec node "${REPO_ROOT}/tools/quality-engineering-harness.js" "$@"
