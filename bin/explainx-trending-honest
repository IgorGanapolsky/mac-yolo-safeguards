#!/usr/bin/env node
'use strict';
const { main } = require('../tools/explainx-trending-honest');
main(process.argv.slice(2)).then(
  (code) => process.exit(code),
  (err) => {
    process.stderr.write(`${err && err.message ? err.message : err}\n`);
    process.exit(1);
  },
);
