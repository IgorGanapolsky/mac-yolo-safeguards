# Chief owns this repo

Google Antigravity is a minion. Chief (Grok Bot chat) is the only driver.

## Product lock
- Public offer: hosted Hermes on a fenced VPS. $10/mo. 14-day trial. Cancel anytime.
- Approvals happen in thumbgate.app.
- Do not hero Continuity. Do not add Mac-pair, phone leash, lid-close, or a RUN ON picker.
- Do not invent traction. Cash is $0 until a stranger pays Stripe.

## Current order
Fix verify on main after PR 1801 merged with a red verify. Update stale tests (`Which machine?`, visible `Open settings`). Do not restore Mac-pair Settings.

## How you take work
1. Wait for a Linear lock (AGENT-*) with files named.
2. Open one PR on that lock.
3. Stop if Chief comments close or wait.
4. Live means click e2e + screenshot, not curl /api/health.

## MCP
Load `chief-command` from `.agents/mcp_config.json`. Call `get_chief_orders` and `get_product_lock` before you edit landing or dashboard copy.
