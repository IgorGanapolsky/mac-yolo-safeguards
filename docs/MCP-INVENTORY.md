# MCP inventory

Generated by `node tools/mcp-health-check.js --write-docs`. Hand-edit `SERVER_META` in the tool when adding servers.

| Server | Locality | Scope | Secret | Blast radius | Config healthy |
|--------|----------|-------|--------|--------------|----------------|
| `github` | remote-http | GitHub Issues/PRs/reviews — agent coordination bus | GITHUB_MCP_PAT / Authorization bearer | repo + org GitHub via token scopes | yes |
| `context7` | remote-http | Library/framework documentation lookup | CONTEXT7_API_KEY (optional) | read-only docs fetch | yes |
| `grepai` | local-stdio | Local semantic code search (Tree-sitter + embeddings) | none (local binary + index path) | read local index only; Mac-only | yes |

## How we know it works

- Structural: `node tools/mcp-health-check.js` (CI / maturity scorecard).
- Optional live: `node tools/mcp-health-check.js --ping` (needs tokens for private servers).
- Product memory: ThumbGate MCP `recall` after `capture` (AGENTS.md protected component).

## Security rules

- Never commit MCP PATs or API keys.
- Prefer least-privilege tokens (GitHub: contents/issues/PRs only as needed).
- Local stdio servers (grepai) must not run on untrusted multi-tenant hosts.

