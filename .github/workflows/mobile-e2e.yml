name: Mobile E2E (Android emulator)

# Tier-1 E2E: runs the Maestro ship-guard critical-path flow on a real Android emulator.
# The fast tier-0 gate (flow validation) already runs on every PR inside CI / mobile-checks.
#
# This job is intentionally on workflow_dispatch only — NOT yet a required PR check — because:
#   1. The two actions marked TODO below must be SHA-pinned to match this repo's security
#      policy (every other action is pinned by commit SHA, not a floating tag).
#   2. Emulator boot + a from-source debug build must be proven green here at least once
#      before it is promoted to a required check via branch protection.
# Promote it by adding `pull_request:` to the triggers and pinning the TODO actions.

on:
  workflow_dispatch:

jobs:
  android-e2e:
    name: Maestro ship-guard (Android emulator)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: hermes-mobile
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: hermes-mobile/package-lock.json

      - uses: actions/setup-java@v4 # TODO: SHA-pin before making this a required check
        with:
          distribution: temurin
          java-version: 17

      - name: Install dependencies
        run: npm ci

      - name: Validate Maestro flows (tier-0, fail fast before the emulator boots)
        run: npm run e2e:validate

      - name: Enable KVM (hardware acceleration for the emulator)
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Install Maestro
        run: |
          curl -fsSL "https://get.maestro.mobile.dev" | bash
          echo "$HOME/.maestro/bin" >> "$GITHUB_PATH"

      - name: Prebuild + assemble debug APK (x86_64 for the emulator)
        run: |
          npx expo prebuild -p android --no-install
          (cd android && ./gradlew assembleDebug -PreactNativeArchitectures=x86_64 --no-daemon)

      - name: Run Maestro ship-guard on emulator
        uses: reactivecircus/android-emulator-runner@v2 # TODO: SHA-pin before making this a required check
        with:
          api-level: 34
          arch: x86_64
          profile: pixel_6
          working-directory: hermes-mobile
          script: |
            adb install -r android/app/build/outputs/apk/debug/app-debug.apk
            export PATH="$HOME/.maestro/bin:$PATH"
            maestro test .maestro/ship-guard.yaml

      - name: Upload Maestro artifacts on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: maestro-debug-output
          path: ~/.maestro/tests/**
          if-no-files-found: ignore
