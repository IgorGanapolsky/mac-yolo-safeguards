---
title: "PR hygiene lesson 2026-06-04: GitHub workflow YAML changes need actionlint plus YAML parser checks before merge; heredoc bodies inside run blocks must be indented for YAML, and completion claims require latest-main CI proof after Trunk merge."
date: 2026-06-04
signal: down
category: "entity:Customer"
tags: 
  - "entity:Customer"
actionType: store-mistake
sourceFeedbackId: fb_1780595491975_xxld2q
---

# PR hygiene lesson 2026-06-04: GitHub workflow YAML changes need actionlint plus YAML parser checks before merge; heredoc bodies inside run blocks must be indented for YAML, and completion claims require latest-main CI proof after Trunk merge.

## Context

PR hygiene lesson 2026-06-04: GitHub workflow YAML changes need actionlint plus YAML parser checks before merge; heredoc bodies inside run blocks must be indented for YAML, and completion claims require latest-main CI proof after Trunk merge.

## Corrective Action

avoid: PR hygiene lesson 2026-06-04: GitHub workflow YAML changes need actionlint plus YAML parser checks before merge; heredoc bodies inside run blocks must be indented for YAML, and completion claims require latest-main CI proof after Trunk merge.

## Tags

[[entity:Customer]]
