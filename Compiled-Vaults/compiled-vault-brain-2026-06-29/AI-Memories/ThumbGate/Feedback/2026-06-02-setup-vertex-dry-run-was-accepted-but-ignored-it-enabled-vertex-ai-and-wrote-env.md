---
title: setup-vertex --dry-run was accepted but ignored; it enabled Vertex AI and wrote .env. Dry-run flags must never mutate cloud state or local config.
date: 2026-06-02
signal: down
category: uncategorized
tags: []
actionType: store-mistake
sourceFeedbackId: fb_1780416477955_v3io82
---

# setup-vertex --dry-run was accepted but ignored; it enabled Vertex AI and wrote .env. Dry-run flags must never mutate cloud state or local config.

## Context

setup-vertex --dry-run was accepted but ignored; it enabled Vertex AI and wrote .env. Dry-run flags must never mutate cloud state or local config.

## Corrective Action

avoid: setup-vertex --dry-run was accepted but ignored; it enabled Vertex AI and wrote .env. Dry-run flags must never mutate cloud state or local config.
