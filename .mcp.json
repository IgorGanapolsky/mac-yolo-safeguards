{
  "$comment": "Shared MCP tools for every agent on this repo. github/context7 are REMOTE/http endpoints so the cloud Replit agent can reach them too (tokens come from env vars, never committed). github = the cross-agent coordination bus (issues/PRs/reviews); context7 = current library docs. grepai is LOCAL-ONLY and now starts through a source-bound freshness gate: stale, torn, tampered, or unchecked generations fail closed so agents fall back to deterministic rg. See docs/LOCAL-SEMANTIC-CODE-INDEX.md.",
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer ${GITHUB_MCP_PAT}"
      }
    },
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "Authorization": "Bearer ${CONTEXT7_API_KEY:-}"
      }
    },
    "grepai": {
      "command": "node",
      "args": [
        "/Users/igorganapolsky/workspace/git/igor/mac-yolo-safeguards/tools/grepai-mcp-fresh.js",
        "serve"
      ]
    }
  }
}
